#include <cs50.h>
#include <ctype.h>
#include <stdio.h>

int count_letters(string text);

int main(void)
{
    string text = get_string("Text: ");
    printf("%s", userInput);
}

int count_letters(string text)
{
    int countText = strlen(text);
    printf("%s letter", countText);
}